import { getJson } from './client.js'; // eslint-disable-line no-unused-vars

// TODO: add routing endpoints here as the module is built, e.g.
// export function fetchRoute(fromNodeId, toNodeId) {
//   return getJson(`/api/routing/route?from=${fromNodeId}&to=${toNodeId}`);
// }
