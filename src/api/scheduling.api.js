import { getJson } from './client.js'; // eslint-disable-line no-unused-vars

// TODO: add shift-scheduling endpoints here as the module is built, e.g.
// export function fetchSchedule(weekStart) {
//   return getJson(`/api/scheduling/shifts?weekStart=${weekStart}`);
// }
